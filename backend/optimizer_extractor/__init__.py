"""Optimizer JSON extractor — drives FSL Optimization Center via Playwright,
uploads request/response JSONs to Azure Blob.

Runs locally on a machine with `sf` CLI authenticated to the prod org (alias 'prod').
"""
