# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Environment variables — ignore EVERY variant, anywhere in the tree.
# Bare ".env" only matches a file named exactly .env; copies/backups like
# ".env copy", ".env.RETIRED-*", "backend/.env.bak" slipped through and each
# carried live Azure/Salesforce/GitHub credentials.
.env
.env*
*.env
*.env.*
backend/.deployment_env.sh

# Node.js / Frontend
frontend/node_modules/
frontend/.vite/
frontend/coverage/

# Claude Code worktrees
.claude/worktrees/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Cache
.cache/

# Deploy artifacts (built locally)
backend/deploy.zip
# Hashed JS/CSS bundles. CI rebuilds backend/static from frontend/dist and zips
# it from the runner's filesystem, so these never need to be in git -- 296 stale
# bundles (362MB) had accumulated here. The non-hashed files in backend/static
# stay tracked: the deploy workflow reads track.html/track_demo.html before it
# wipes the directory.
backend/static/assets/
*.pdf
backend/migrate_passwords_to_bcrypt.py

# Local artifacts / temp files
backend/-
WO-Adj/
reports/
.claude/scheduled_tasks.lock
.claude/settings.local.json
backend/migrate_users_to_postgres.py
data/
tmp/
doc/

# Generated report artifacts
output/
